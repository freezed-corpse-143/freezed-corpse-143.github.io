# paddlenlp+paddlepaddle-gpu 安装

> 环境要求：linux 环境


1. 配置环境

```bash
sudo apt install -y build-essential cmake zlib1g-dev python3-dev python3.12-dev
```

2. 初始化 python 虚拟环境，并激活

```bash
uv venv --python 3.12
```

3. 安装

```bash
uv pip install paddlepaddle-gpu
uv pip install paddlenlp
```