
# 最大堆/最小堆

堆是一棵完全二叉树，用数组存储，满足：

- 大顶堆：每个父节点 ≥ 子节点 → 堆顶是最大值
- 小顶堆：每个父节点 ≤ 子节点 → 堆顶是最小值。

数组下标对应关系

```
父节点 i → 左孩子 2i+1, 右孩子 2i+2
子节点 i → 父节点 (i-1) //2
```

完整实现

```python

class Heap:

	def __init__(self, is_max=True):
		self.data = []
		# 大顶堆、小顶堆
		self.is_max = is_max
	
	# 唯一的参数，比较规则
	def _ok(self, parent, child):
		if self.is_max:
			return parent >=child
		else:
			return parent <=child
	
	# 上浮：新元素放到末尾后向上调整	
	def _sift_up(self, i):
		while i > 0:
			parent = (i-1) //2
			if self._ok(self.data[parent], self.data[i]):
				break
			self.data[parent], self.data[i] = self.data[i], self.data[parent]
			i = parent
			
	# 下沉：堆顶被替换后向下调整
	def _sift_down(self, i):
		n = len(self.data)
		while True:
			left, right =  2*i+1, 2i+2
			# 该交换的元素
			target = i
			# 左孩子该去堆顶
			if left < n and not self._ok(self.data[target], self.data[left]):
				target = left
			if right < n and not self._ok(self.data[target], self.data[right]):
				target = right
			if target == i:
				break
			self.data[i], self.data[target] = self.data[target], self.data[i]
			i = target
			
	# 末尾插入元素，上浮
	def push(self,val):
		self.data.append(val)
		self._sift_up(len(self.data)-1)
		
	# 弹出堆顶元素
	def pop(self):
		if not self.data:
			raise IndexError("heap is empty")
		# 取堆顶元素	
		top = self.data[0]
		# 再取末尾元素
		last = self.data.pop()
		if self.data:
			# 移动末尾元素，放到堆顶
			self.data[0] = last
			self._sift_down
		return top
		
	def __len__(self):
		return len(self.data)
	
```