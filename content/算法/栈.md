# 最大栈/最小栈

普通栈只有 push / pop / top，只能 O(1) 拿到栈顶

最大栈再次在这个基础上多个 getMax()，O(1)，拿到整个栈的最大值。

核心思路：如果要弹出栈中的最大值，需要弹出其前面的所有值，所以如果弹出栈中的最大值，我们只需要维护最大值后面的子序列中的最大值。同理，如果压栈，只需要对比该值与当前最大值，保存一个最大值栈即可。

完整代码

```python
class MaxMinStack2:
	def __init__(self, is_max=True):
		self.is_max = is_max
		self.data = []
		# 保存最值序列
		selft.aux = []
		
	def _better(self, a, b):
		return (a >= b) if self.is_max else (a <= b)
		
	def push(self, val):
		self.data.append(val)
		if not self.aux or self._better(val, self.aux[-1]):
			self.aux.append(val)
			
	def pop(self):
		if not self.data:
			raise IndexError("empty")
		val = self.data.pop()
		if self.aux and val == self.aux[-1]:
			self.aux.pop()
		return val
		
	def get_extreme(self):
		return self.aux[-1] if self.aux else None
```